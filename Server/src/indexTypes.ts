export interface fromAppTypes {
    userFirebaseID: string;
    recipientFirebaseID: string;
    timeStamp: string;
    message: string;
}

export interface newUser {
    email: string;
    uid: string;
}
